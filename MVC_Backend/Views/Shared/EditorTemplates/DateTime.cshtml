@model DateTime?
@{
    var value = Model.HasValue
                    ? Model.Value.ToString("yyyy-MM-dd HH:mm")
                    : "";
}
@Html.TextBox("", value, new
{
    @class = "form-control",
    data_date_format = "YYYY-MM-DD HH:mm"
})